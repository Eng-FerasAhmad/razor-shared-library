import { ReactElement, ReactNode } from '../../../../../node_modules/react';
interface Props {
    headerTools: ReactNode;
}
export default function EntityTableToolbar({ headerTools, }: Props): ReactElement;
export {};
