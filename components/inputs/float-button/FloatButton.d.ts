import { ReactElement, ReactNode } from '../../../../node_modules/react';
import { FabProps } from '@mui/material/Fab';
interface Props {
    icon: ReactNode;
}
export declare function FloatButton({ icon, ...props }: Props & FabProps): ReactElement;
export {};
